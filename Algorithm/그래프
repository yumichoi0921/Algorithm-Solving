# 최단(최적)경로일 때
1. 하나의 시작 정점에서 끝 정점까지 최단(최적) 경로 (가중치=비용)
    그래프 가중치에 음의 값이 포함된 경우 -> bellman-ford
    그래프 가중치가 모두 양의 값이라면 -> Dijkstra
    가중치가 없다면(거리만 따진다면) -> BFS
2. 모든 정점들에 대한 최단 경로
    Floyd Warshall

# Minimum Spanning Tree
    1. 간선이 많다면 Prim
    2. 간선이 적다면 Kruskal